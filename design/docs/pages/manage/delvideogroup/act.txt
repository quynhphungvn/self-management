@startuml 
start
:Browser;
:Enter input;
:Check input;
:Send request delete;
:Server;
if (check param) then (valid)
 :Delete group;
 if (delete success) then (ok)
  :Response delete ok;
 else (not ok)
  :Response delete fail;
 endif
else (invalid)
 :Response param invalid;
endif
:Browser;
if (Message) then (success)
 :Update Gui;
else (fail)
 :Show fail message;
endif
end
@enduml
