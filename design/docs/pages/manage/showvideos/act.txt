@startuml 
start
:Browser;
:Select Group;
:Enter gname to input;
:Send request videos by groupId;
:Server;
if (check groupId) then (valid)
 :Get videos;
 if (get success) then (ok)
  :Response data;
 else (not ok)
  :Response null;
 endif
else (invalid)
 :Response param invalid;
endif
:Browser;
if (Message) then (success)
 :Update Gui;
else (fail)
 :Show fail message;
endif
end
@enduml
